config/config.php
/vendor/
*.log
node_modules/
package-lock.json
Trazock-handoff.zip

codigo.jpg
*.jpg

# …pero los assets de imagen de la app SÍ se versionan (logo del cliente, etc.)
!assets/img/*.jpg

# Fuentes de Claude Design (no se versionan)
design/

# Fotos de remitos firmados (entregas): se acumulan, no van al repo.
# Se conserva el .htaccess que bloquea el acceso web directo.
/storage/remitos/*
!/storage/remitos/.htaccess
